# Grafana Dashboard Provider Configuration

apiVersion: 1

providers:
  - name: 'ValuerPro Dashboards'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards